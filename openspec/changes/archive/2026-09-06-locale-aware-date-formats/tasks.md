## 1. Format helper

- [x] 1.1 Update Spanish output in `formatBookDate` to use `/` as the separator (`dd/mm/yyyy`)
- [x] 1.2 Run `npx tsc --noEmit`, `npm run lint`, `npm run build` — all clean
- [x] 1.3 Run a one-off script to confirm `formatBookDate("2026-08-13", "es")` returns `13/08/2026` and the same input with `"en"` returns `2026-08-13`
